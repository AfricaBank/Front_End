const ProgressBarStyles = {
  Progress: {
    parts: ["track", "filledTrack", "label"],
    defaultProps: {
      variant: "unstyled",
    },
    baseStyle: {
      track: {
        overflow: "initial",
      },
    },
  },
};
export default ProgressBarStyles;
